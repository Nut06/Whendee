# Frontend environment variable examples
# Copy this file to .env and adjust values as needed.

# Explicit service base URLs (used if set). If omitted, fallback to port-based URLs.
IDENTITY_SERVICE_URL=http://localhost:3002
COMMUNICATION_SERVICE_URL=http://localhost:3003
EVENT_SERVICE_URL=http://localhost:3004

# Optional: override service ports instead of full URLs
IDENTITY_SERVICE_PORT=3002
COMMUNICATION_SERVICE_PORT=3003
EVENT_SERVICE_PORT=3004

# =============================
# Dev Auth Override
# =============================
# Enable sending a raw userId for development without requiring a JWT access token.
# NEVER enable this in production builds.
EXPO_PUBLIC_DEV_AUTH_OVERRIDE=false

# When override enabled, this userId will be injected as x-user-id header and
# added to POST/PUT/PATCH JSON bodies that lack userId/id.
EXPO_PUBLIC_DEV_USER_ID=00000000-0000-0000-0000-000000000000

# Notes:
# - Set EXPO_PUBLIC_DEV_AUTH_OVERRIDE=true only in local dev.
# - Use a real user id from your dev database.
# - Access token (if present) still takes precedence for backend logic.
# - Interceptor won't overwrite existing userId or id fields.
